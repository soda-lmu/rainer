# Provides an explanation for an error message

Use that function to get an explanation for an error message as well as
a suggestion how to correct the issue.

## Usage

``` r
r_error(error = TRUE)
```

## Arguments

- error:

  Logical. If FALSE, excludes the last error message in the query. TRUE
  per default.

## Value

Response
