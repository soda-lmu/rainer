# Addin with r_error, which provides an explanation for an error message

This function creates the addin in order to execute r_error in the
addins menu, used internally

## Usage

``` r
r_error_addin()
```

## Value

Nothing
