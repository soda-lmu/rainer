# Function to get improvement suggestions

Use that function to get improvement suggestions on your current script.

## Usage

``` r
r_improve(error = FALSE)
```

## Arguments

- error:

  Logical. If TRUE, adds the last error message to the output. FALSE per
  default.

## Value

Response
