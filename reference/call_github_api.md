# Function to send the information and prompt to GPT via github

This function sends takes care of sending the information and the prompt
and is used internally

## Usage

``` r
call_github_api(body)
```

## Arguments

- body:

  The body of the message, containing both the information and the
  prompt

## Value

Nothing
