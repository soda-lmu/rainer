# Function for validating the GitHub token

This function checks whether the provided GitHub token is valid for
accessing the LLMs and therefore for using rainer.

## Usage

``` r
github_token_validation(response_token)
```

## Arguments

- response_token:

  The token provided by the user which is about to be checked

## Value

Nothing
